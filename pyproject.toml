[tool.poetry]
name = "wise-agents"
version = "0.0.1"
description = "Multi-agent framework"
authors = []
readme = "README.md"
packages = [{include = "wise-agents"}]

[tool.poetry.dependencies]
python = ">=3.8.1,<4.0"